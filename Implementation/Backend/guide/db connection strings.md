# Database Connection Strings 
We have to use different connection strings because we don't have a dedicated database host.  
Each person should have MS SQL Server installed and have their own connection string to use database in backend.  
Here's each person's connection string:
## conneciton strings
### Abolfazl
```json
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=DESKTOP-GUGI795; Initial Catalog=ReserveCenterDB;Integrated Security=true;MultipleActiveResultSets=true;TrustServerCertificate=True;"
  },
```

### Sajad

```json

```

